# Copy this file to .env and fill your secrets.
# Example: cp .env.example .env

# Choose LLM provider: openai | ollama
LLM_PROVIDER=ollama

# OpenAI settings (when LLM_PROVIDER=openai)
# OPENAI_API_KEY=sk-...
# Optional: choose the model (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o

# Ollama settings (when LLM_PROVIDER=ollama)
OLLAMA_MODEL=gemma3:4b

# Optional: set Ollama base URL (default: http://localhost:11434)
# OLLAMA_BASE_URL=http://localhost:11434

# Optional Bing Search key. If unset, falls back to Wikipedia OpenSearch
# BING_API_KEY=...

# Optional default question when running __main__
# QUESTION=Who is Milhouse named after in The Simpsons?
